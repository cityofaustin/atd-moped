import React from "react";
import { Grid, CardContent } from "@material-ui/core";

const ProjectTabPlaceholder = () => {
  return (
    <CardContent>
      <Grid container spacing={2}>
        TBD
      </Grid>
    </CardContent>
  );
};

export default ProjectTabPlaceholder;
