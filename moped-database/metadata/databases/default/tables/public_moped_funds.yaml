table:
  name: moped_funds
  schema: public
select_permissions:
  - role: moped-admin
    permission:
      columns:
        - id
        - fund_id
        - fund_name
        - is_deleted
      filter: {}
  - role: moped-editor
    permission:
      columns:
        - id
        - fund_id
        - fund_name
        - is_deleted
      filter: {}
  - role: moped-viewer
    permission:
      columns:
        - id
        - fund_id
        - fund_name
        - is_deleted
      filter: {}
