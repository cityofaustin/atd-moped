name: "Build eCapris funding ETL Docker image and push to DockerHub"
on:
  push:
    branches:
      - main
      - production
    paths:
      - ".github/workflows/etl_ecapris_funding_docker.yml"
      - "moped-etl/ecapris-funding/**"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  build:
    name: Build eCapris funding sync ETL docker image
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Build and push image
        uses: docker/build-push-action@v4
        with:
          platforms: linux/amd64
          push: true
          tags: atddocker/atd-moped-etl-ecapris-funding:${{ github.ref_name }}
          context: ./moped-etl/ecapris-funding
