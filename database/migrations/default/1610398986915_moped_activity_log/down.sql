drop table moped_activity_log;

drop index if exists moped_activity_log_record_id_index;

drop index if exists moped_activity_log_record_type_id_index;

drop index if exists moped_activity_log_updated_by_id_index;

drop index if exists moped_activity_log_updated_by_index;
